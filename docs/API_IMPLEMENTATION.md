# 9288S 系统 API 实现清单

## 范围与证据

本清单只记录兼容层实际提供的 9288S 系统接口，不把“能进入某个画面”等同于
“API 已完整实现”。

接口名称、顺序、参数和返回值以原始 9288S 下载程序开发环境为依据：

`D:\Downloads\app_env_9288s_\app_env_9288s\Down_Include\RT_table`

重点参考文件：

- `RT_GUI.h`：`T_GUI_RelocationTable`
- `RT_FS16.h`：`T_FS_RelocationTable`
- `RT_OS.h`、`RT_Audio.h`、`RT_Dict.h`
- `gui/guiExt.h`、`gui/guiGdi.h`、`gui/guiWindow.h`

逐游戏次数来自当前仓库 `build/game-matrix` 中的动态探针日志。因此它们是
“已执行路径覆盖”，不是静态证明；尚未进入的菜单、关卡或异常路径仍可能调用新的
API。发布前必须同时完成静态导入扫描和逐路径动态回归。

开发环境还提供了一个带源码、ELF 和链接 Map 的官方样例：

`D:\Downloads\app_env_9288s_\app_env_9288s\build\测试.exe`

它已作为独立 ABI 回归程序接入宿主探针。当前可以完成创建主窗口、枚举当前目录、
创建并选择 12 像素字体、绘制 3D 控件框和文字，随后进入 `GetMessage` 消息循环，
没有未识别的系统槽位。该结果验证的是官方样例覆盖到的路径，并不代替四款游戏的
逐路径测试。

状态含义：

- **已实现**：参数、返回值和当前已覆盖路径的主要副作用均已实现。
- **部分实现**：当前路径可运行，但仍有缺失语义、固定返回值或仅支持有限对象。
- **等价空操作**：原调用在当前单窗口兼容模型中不需要额外工作，但保留正确返回值。
- **占位**：仅防止程序因未支持 API 退出，不能视为兼容完成。
- **待实机**：9588 模拟器已验证，尚未由 9588 真机结果确认。

## 9588 承接策略

- 图形：9288S 2bpp 图像、DC 状态和绘图原语先写入 RGB565 离屏帧缓冲，
  默认在 `EndPaint/UpdateWindow` 批量提交；整个 BDA 生命周期复用一个 9588
  原生 Frame，程序列表与 EXE 切换时不销毁重建绘图上下文。
- 输入：直接读取 9588 原始触摸事件和按键包，再生成 9288S 的鼠标、键盘消息；
  不依赖网页模拟器的输入协议。
- 计时：`SetTimer/KillTimer` 按官方 `(hwnd,id)` 语义管理最多 16 个计时器，
  使用 9588 SDK 单调时钟推进并向原 EXE 消息队列投递 `MSG_TIMER`。
- 文件：所有客体路径统一限制在 `应用/数据/9288s` 根目录内，保留原始目录层级，
  不按游戏名或存档名写死。
- 帮助：9288S `Help2` 由 9588 固件 Help Page 承接，不转换成消息框。

## 四款程序的动态调用矩阵

表内数字为该探针路径的调用次数，`—` 表示该路径未观察到调用。

| 表:槽位 | 9288S 原型名 | 五子棋 | 贪食蛇 | 连连看 | 雷霆战机 | 当前状态 | 备注 |
|---|---|---:|---:|---:|---:|---|---|
| GUI:12 | `GetMessage` | 65 | 65 | 65 | 65 | 部分实现、待实机 | 事件队列、按键、触摸和定时器已接入；仍需真机时序回归 |
| GUI:16 | `SendMessage` | — | — | 62 | — | 部分实现 | 主窗口和兼容层列表框消息已实现，不是通用窗口系统 |
| GUI:20 | `TranslateMessage` | 64 | 64 | 64 | 64 | 等价空操作 | 当前 9288S 键消息无需二次转换 |
| GUI:21 | `DispatchMessage` | 64 | 64 | 64 | 64 | 已实现、待实机 | 调用原 EXE 的窗口过程 |
| GUI:29 | `SetInstantPaint` | — | 132 | — | 26 | 已实现、待实机 | 默认离屏批量绘制并在 `EndPaint/UpdateWindow` 提交；开启后每个绘图调用立即提交 |
| GUI:33 | `CreateMainWindow` | 1 | 1 | 1 | 1 | 部分实现 | 支持游戏主窗口及一层存档/读档窗口 |
| GUI:35 | `DefaultMainWinProc` | 72 | 72 | 134 | 72 | 占位 | 当前返回 0；需要补齐未处理消息的默认行为 |
| GUI:37 | `UpdateWindow` | 3 | — | 72 | — | 已实现、待实机 | 提交兼容帧缓冲 |
| GUI:56 | `InvalidateRect` | 731 | — | 129 | 1297 | 已实现、待实机 | 合并同一窗口的待处理重绘，并抑制 `MSG_PAINT` 回调内部的重复无效化，避免重绘消息饥饿计时器与输入 |
| GUI:57 | `BeginPaint` | — | — | 64 | — | 部分实现 | 当前返回主 DC；尚未实现独立 DC 状态 |
| GUI:58 | `EndPaint` | — | — | 64 | — | 已实现、待实机 | 结束绘制并提交 |
| GUI:61 | `ClientToScreen` | — | — | 127 | 1 | 部分实现 | 顶层窗口坐标等价空操作；嵌套窗口坐标尚不完整 |
| GUI:107 | `SetTimer` | 3 | 2 | 2 | 2 | 已实现、待实机 | `SetTimer(hwnd,id,speed)`；按官方 `DEF_NR_TIMERS` 支持最多 16 个按 `(hwnd,id)` 区分的并发计时器；由 9588 SDK 的 25ms 单调时钟驱动，按官方 `TIME_MS(ms)=ms*10/25` 反算每个 `speed` 单位为 2.5ms |
| GUI:108 | `KillTimer` | 2 | 3 | 1 | 1 | 已实现、待实机 | 精确按 `(hwnd,id)` 注销，不影响同 ID 的其他窗口计时器；返回是否找到目标 |
| GUI:191 | `GetSysPixelIndex` | 8 | 63 | 135 | 8 | 部分实现 | 已覆盖四级灰度常用索引；完整系统色表待核对 |
| GUI:192 | `GetGDCapability` | — | 2 | — | — | 部分实现 | 只提供 160×240、2bpp 等已用能力项 |
| GUI:193 | `GetDC` | 1 | 66 | 1 | 1 | 部分实现 | 当前统一返回主 DC |
| GUI:195 | `ReleaseDC` | — | 66 | — | — | 等价空操作 | 统一主 DC 无需释放 |
| GUI:205 | `SetBkColor` | — | — | 67 | — | 已实现、待实机 | 保存并返回旧背景色 |
| GUI:206 | `SetBkMode` | — | — | — | 4 | 已实现、待实机 | 原定义：`BM_OPAQUE=0`、`BM_TRANSPARENT=1`；不透明模式会按当前背景色填充字格 |
| GUI:207 | `SetTextColor` | — | — | 67 | 4 | 已实现、待实机 | 保存并返回旧文字色 |
| GUI:214 | `SetPenColor` | 4 | — | — | 7 | 已实现、待实机 | 线条和矩形边框 |
| GUI:215 | `SetBrushColor` | — | 62 | — | 4 | 已实现、待实机 | `FillBox` 填充色 |
| GUI:223 | `LineTo` | — | — | — | 6 | 已实现、待实机 | 与 `MoveTo` 共享当前位置 |
| GUI:224 | `MoveTo` | — | — | — | 6 | 已实现、待实机 | 保存当前画笔坐标 |
| GUI:227 | `Rectangle` | 4 | — | — | 1 | 已实现、待实机 | 9288S 语义为只画边框 |
| GUI:254 | `SaveScreenBox` | — | — | 127 | 1 | 已实现、待实机 | 按 9288S 2bpp 行步长写入 EXE 提供的客体缓冲区 |
| GUI:256 | `PutSavedBoxOnScreen` | — | — | 127 | 2 | 已实现、待实机 | 从客体 2bpp 缓冲区恢复，支持裁剪 |
| GUI:259 | `FillBox` | — | 62 | — | 8 | 已实现、待实机 | 参数为 `hdc,x,y,w,h` |
| GUI:268 | `SetRect` | — | 140 | — | — | 已实现 | 写入四个 32 位坐标 |
| GUI:283 | `PtInRect` | — | 140 | — | — | 已实现 | 当前按包含边缘处理，需和原库边界测试复核 |
| GUI:316 | `TextOutLen` | — | 248 | — | — | 部分实现 | ASCII/GBK 点阵已实现；背景模式、字体度量仍需补齐 |
| GUI:318 | `DrawTextEx` | — | — | 67 | 8 | 部分实现 | 基本文字和裁剪已实现；格式标志尚未完整实现 |
| GUI:336 | `PutImageArea` | 727 | 882 | 259 | 2 | 已实现、待实机 | 2bpp SDK 图片对象；以 16 字节资源头的宽、高和载荷长度确定行步长。五子棋选项页会传差值形式的 `w/h`，不能直接作为图片行宽 |
| GUI:351 | `GetCurrDateTime` | — | 1 | 2 | 1 | 占位 | 当前返回固定时间，需接入 9588 RTC |
| GUI:355 | `SysShowPicV` | — | — | — | 2436 | 已实现、待实机 | 2bpp 虚拟屏；模式 0=覆盖、1=与、2=或、3=非 |
| GUI:356 | `SysShowPicS` | — | — | — | 27 | 已实现、待实机 | 2bpp 直接显示并支持裁剪/逻辑模式 |
| GUI:358 | `GetPartPicData` | — | — | — | 3 | 已实现、待实机 | 截取范围包含右下边缘 |
| GUI:362 | `SysPrintString` | — | — | — | 20 | 部分实现 | 写入虚拟屏；汉字/ASCII 点阵已接入 |
| GUI:364 | `ClearRect` | 1 | — | — | — | 已实现、待实机 | 按原 API 包含右下边缘 |
| GUI:388 | `ShowStatusBarAndDesktop` | 1 | 1 | 1 | 1 | 占位 | 兼容 BDA 固定保留自身控制面板，尚未模拟原系统桌面状态 |
| GUI:390 | `ReverseRect` | — | 1 | — | — | 已实现、待实机 | 按 `x,y,w,h` 反显 |
| GUI:404 | `MusicVolumeGet` | — | — | — | 1 | 占位 | 当前固定返回值，音频表尚未实现 |
| GUI:419 | `ScanKeyForGameComboKeys` | — | — | — | 21 | 已实现、待实机 | 6 字节顺序：右、左、下、上、取消、确认 |
| GUI:430 | `ResetAutoCloseTimer` | — | 39 | — | 24 | 占位 | 当前不操作；真机需要调用 9588 的待机计时重置 |
| GUI:437 | `GetScrWidth` | — | — | 3 | — | 已实现 | 返回 160 |
| GUI:438 | `GetScrHeight` | — | — | 3 | — | 已实现 | 返回 240 |
| GUI:455 | `AttachEnable` | — | — | — | 1 | 占位 | 当前不改变 USB attach 状态 |
| FS:0 | `fopen` | 2 | — | — | 1 | 已实现、待实机 | 统一映射到 `A:\应用\数据\9288s` 根目录 |
| FS:1 | `fclose` | 2 | — | — | 1 | 已实现、待实机 | 9588 文件句柄校验 |
| FS:2 | `fread` | 1 | — | — | 2 | 已实现、待实机 | 客体/宿主缓冲区复制 |
| FS:4 | `fseek` | — | — | — | 2 | 已实现、待实机 | 保留 9288S 返回位置语义 |
| FS:5 | `ftell` | — | — | — | 1 | 已实现、待实机 | 返回当前位置 |

## 官方开发样例补充覆盖

这些接口没有出现在上面四款游戏的已执行路径中，但由开发包的
`SRC/downsample.c` 和实际运行的 `测试.exe` 同时确认：

| 表:槽位 | 原接口 | 当前状态 | 已验证行为 |
|---|---|---|---|
| GUI:10 | `Draw3DControlFrame` | 已实现、待实机 | `hdc,x0,y0,x1,y1,fillc,updown`；填充及凹凸四边 |
| GUI:298 | `SelectFont` | 已实现、待实机 | 返回旧字体，选择 `CreateLogFont` 创建的 12 像素字体 |
| FS:19 | `getcwd` | 已实现、待实机 | 支持调用者缓冲区和 `buffer=NULL` 的客体堆分配 |

## FS16 完整槽位清单

以下顺序直接来自开发包 `RT_FS16.h`。这也修正了早期兼容层把槽位 21/27
误认的错误：21 是 `stat`，27 才是 `access`。

| 槽位 | 原接口 | 当前状态 | 说明 |
|---:|---|---|---|
| 0–5 | `fopen/fclose/fread/fwrite/fseek/ftell` | 已实现、待实机 | 客体缓冲复制，所有路径映射到私有根目录 |
| 6 | `feof` | 部分实现 | 9588 无独立入口，当前依赖读取计数，直接查询固定返回未到 EOF |
| 7–8 | `ferror/clearerr` | 部分实现 | `ferror` 接 9588；`clearerr` 为兼容空操作 |
| 9–13 | `remove/rename/chdir/mkdir/rmdir` | 已实现、待实机 | 相对路径跟随客体当前目录，禁止越出根目录 |
| 14 | `getfn` | 占位 | 当前返回 0，尚未生成原固件文件编号 |
| 15–17 | `findfirst/findnext/findclose` | 已实现、待实机 | 支持 GBK 文件名、属性和当前目录 |
| 18 | `get_free` | 部分实现 | 返回足够的持久空间，尚未读取 9588 实际剩余容量 |
| 19–20 | `getcwd/getcwd2` | 已实现、待实机 | 返回 9288S 虚拟路径，不泄漏 9588 原生前缀 |
| 21 | `stat` | 部分实现、待实机 | 文件类型和大小已实现，时间字段暂为 0 |
| 22 | `init` | 等价空操作 | BDA 打开时 9588 文件系统已初始化 |
| 23 | `format` | 未实现 | 出于隔离和安全要求，不允许客体格式化 9588 存储 |
| 24 | `refresh_cache` | 等价空操作 | 9588 文件 API 直接提供一致视图 |
| 25–26 | `read_cluster/abs_read` | 未实现 | 不向客体暴露 9588 原始扇区 |
| 27 | `access` | 已实现、待实机 | 在虚拟根目录内检查文件/目录存在性 |
| 28–29 | `update/flush_cache` | 等价空操作 | 当前写入由 9588 文件 API 提交 |

动态日志合计：

- 五子棋：1756 次，19 个不同槽位
- 贪食蛇：2176 次，22 个不同槽位
- 连连看：1708 次，25 个不同槽位
- 雷霆战机：4197 次，39 个不同槽位
- 四款程序合并：49 个不同 GUI/FS 槽位

## CRTL 内存接口

CRTL 表在通用分发层内部处理，因此不出现在上面的 GUI/FS 探针计数中。

| 槽位 | 原接口 | 当前状态 | 缺口 |
|---:|---|---|---|
| 0 | `malloc` | 已实现 | first-fit 分配并复用释放块 |
| 1 | `free` | 已实现 | 释放块可被后续分配复用 |
| 2 | `calloc` | 已实现 | 溢出检查、分配和清零 |
| 3 | `realloc` | 已实现 | 原地扩展或迁移并复制旧内容 |
| 4 | `ansi_InitMalloc` | 已实现 | 重设客体堆区间 |

当前分配器最多跟踪固定数量的活动块；长时间运行仍需记录峰值和耗尽情况。

## C33→MIPS32 JIT

9588 端包含真实的基本块 JIT，不是把解释器改名：

- 把已支持的 S1C33 算术、逻辑、移位、扩展、乘法、特殊寄存器和 PSR 指令生成
  MIPS32 机器码；
- 支持最多两个立即数前缀与随后的寄存器/栈内存读写合并；内存访问仍通过边界检查
  助手执行，失败时安全回退解释器；
- 支持条件分支以及可编译算术延迟槽，分支条件在延迟槽执行前保存，PC 和指令数按
  S1C33 延迟分支语义更新；尚未支持的控制流和系统陷阱回退解释器；
- 使用 4096 个基本块槽位、16384 槽开放寻址哈希表和 1 MiB 可执行代码缓存；
  已编译块按 PC 平均 O(1) 查找，不支持的 PC 也进入负缓存，避免每次循环重新扫描
  及重复编译失败；
- 按 `gba-for9588` 的做法以 32 字节 cache line 同步 D-cache 和 I-cache；
- 每个新生成块第一次执行时，同时跑解释器参考路径和原生块，逐项比较寄存器、
  PC、PSR、SP、ALR/AHR、扩展状态和指令计数；
- 参考路径按实际解释器指令计数推进，兼容一次 `c33_vm_step` 内执行分支和延迟槽
  两条指令的情况；
- 校验不一致时立即恢复解释器结果并禁用该块，不让错误状态继续污染画面；
- `9288LOG.TXT` v10 中的三个 `JIT_HEARTBEAT` 记录编译、校验失败、命中/未命中、
  回退、原生指令、负缓存和哈希探测数据；退出程序时由 `JIT_STATS_A` 至
  `JIT_STATS_E` 输出完整统计。心跳从每 2 秒降为每 5 秒一次，减少真机文件写入
  干扰。

用户提供的 v4 真机日志显示，旧实现会出现数十万次线性查找未命中及上百万次
解释器回退。对应优化完成后的 9588 模拟器回归结果：

- 五子棋：编译 1311 个块，约 248 万次缓存命中；JIT 原生执行约 668 万条，
  解释器回退约 198 万条，已覆盖指令中约 77% 走原生块；校验失败 0。
- 雷霆战机小缓存基线：512 槽位导致 393 次全缓存清空和约 20.1 万次重复编译。
- 雷霆战机 4096 槽版本：同类覆盖路径编译 1603 个块，缓存清空 0，重复编译量
  减少约 99.2%；约 1407 万条原生、376 万条回退，原生覆盖约 79%；校验失败 0。

这些数字证明模拟器覆盖到的块语义一致并消除了已知的编译抖动，不等同于 9588
真机性能验收；v10 BDA 仍需用相同游戏路径采集真机日志比较。

## 画面逐像素对照

五子棋的 9288S 参考帧位于 `build/game-matrix/reference-9288s`。对无动态棋子帧，
把 9588 画面中的 160×240 客体区域裁出并映射为四级灰度后：

- 整帧差异：140 / 38,400 像素（0.36%）；
- 棋盘区差异：0.08%；
- 六个游戏选项按钮区差异：0 像素；
- 剩余差异集中在步数、状态等动态文字。

因此当前模拟器里“五子棋游戏选项花屏”已经不能复现；用户提供的真机照片仍是
有效失败证据，必须用本次重新构建的 BDA 在真机复测，不能用模拟器结果替代。

## 尚未覆盖的系统表

- ROS33：四款游戏当前已执行路径未观察到外部槽位调用。
- AUDIO：雷霆战机当前只通过 GUI 表查询音量，尚未观察到 Audio 表调用。
- DICT：未观察到调用。
- FS 的写入、建目录、枚举、磁盘信息等路径已经为存档兼容实现，但四款游戏的上述
  探针路径没有全部触发；必须在每个游戏的真实存档/读档路径单独验证。

## 发布门槛

每个目标程序发布前必须满足：

1. 静态导入扫描和全部可达菜单的动态 API 记录没有未识别槽位。
2. 所有“占位”调用均证明对该程序无副作用，或改为真实实现。
3. 原 9288S 模拟器与 9588 兼容层的关键帧逐像素比较通过。
4. 9588 模拟器记录 API、解释器/JIT、帧提交和定时器性能数据。
5. 9588 真机逐项复测；模拟器通过不能标记为真机通过。
